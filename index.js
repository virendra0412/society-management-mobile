import { registerRootComponent } from "expo";
import App from "./src/App";

// registerRootComponent handles AppRegistry.registerComponent()
// and ensures the correct environment (Expo Go, standalone, web)
registerRootComponent(App);